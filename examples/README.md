Some examples that are mostly motivated by text book exercises or contents. This also contains a
python environment that is managed by `uv`. Use `uv sync` to install the dependencies and then you
have visualisation scripts that help you visualise the results.
